<div class="hero-unit">
		<br />
		<br />
	<div id="navcontainer">
			
	<ul class="idTabs">
		 
	  <li><a href="#jquery">Online Streams</a></li> 
	  <li><a href="#official">Offline Streams</a></li> 
	     </ul>
</div>
	<div id="jquery">
		<% if @streams.each %>
		<% @streams.each do |f| %>
		<% if is_online(f.link) %>
		<li><%=  link_to f.link , f%></li> 
			<% end %>
			<br />
		<%end %>
		<% end%>
		<br />
		<br /><br />
		</div>
		
	<div id='official'>
		<% if @streams.each %>
		<% @streams.each do |f| %>
		<% if is_online(f.link) == nil  %>
<li>  <%=  link_to f.link , f%></li>	
			<% end %>
			<br />
		
		<%end %>	<br />
			<br />
		<% end%>
			
			
			
		</div>
	
	
	</div>
